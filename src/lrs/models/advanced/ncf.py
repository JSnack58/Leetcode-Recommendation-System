"""Neural Collaborative Filtering (NCF).

Deep learning approach to capture non-linear user-problem interactions.
Implements the BaseRecommender interface using PyTorch.

Reference: He et al., "Neural Collaborative Filtering" (WWW 2017)
"""

# TODO: Implement NCF model
# Architecture: concatenate user + problem embeddings → MLP layers → sigmoid output
# Requires: torch (in [advanced] dependency group)
# Note: install with `uv sync --group advanced`
