"""SVD-based collaborative filtering baseline.

Uses matrix factorization via the `implicit` or `surprise` library.
Implements the BaseRecommender interface.
"""

# TODO: Implement SVD model
# Options: surprise.SVD or scipy.sparse.linalg.svds
# Write ADR when choosing between libraries (surprise vs implicit vs custom)
