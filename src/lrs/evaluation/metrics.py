"""Offline evaluation metrics for recommendation quality."""

# TODO: Implement metrics
# Required metrics:
#   ndcg_at_k(relevant, recommended, k)    -> float
#   precision_at_k(relevant, recommended, k) -> float
#   mean_reciprocal_rank(relevant, recommended) -> float
#   coverage(recommended, all_problems)    -> float  (catalog coverage)
#   novelty(recommended, popularity)       -> float  (avg inverse popularity)
